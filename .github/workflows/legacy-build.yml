name: "Legacy iOS Build"
# The ios-build/Timestretch Xcode project was removed in the repo restructure
# (commit ebc7175). This workflow is now a no-op to avoid false failures.
# The active build+test workflow is ios-build.yml.
on:
  workflow_dispatch:
jobs:
  build:
    name: Build (legacy ios-build/Timestretch — retired)
    runs-on: ubuntu-latest
    steps:
      - name: No-op
        run: echo "Legacy Timestretch Xcode project was removed. Nothing to build."
